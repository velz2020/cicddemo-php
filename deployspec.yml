version: 0.1

files:
  - source: /
    destination: /var/www/cicd-demo/backend

scripts:
  BeforeDeploy:
    - location: preScript.sh
      timeout: 300
      runas: root

  AfterDeploy:
    - location: postScript.sh
      timeout: 300
      runas: root